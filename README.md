# Log-Analyzer
Digital Forensics Log Analyzer is an OOP-based C++ tool that parses unstructured logs (like Linux auth.log), automates threat detection (brute-force &amp; root escalations), and provides a complete case management console for incident response.
